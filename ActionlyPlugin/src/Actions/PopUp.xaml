<Window x:Class="Loupedeck.ActionlyPlugin.PopUpWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="" Height="150" Width="300"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        ShowInTaskbar="False" Topmost="True" Loaded="Window_Loaded">
    <Border CornerRadius="8" Background="#232423" Padding="0" Effect="{DynamicResource _DropShadow}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <!-- Top-right Close X -->
            

            <!-- Dynamic UI region -->
            <ContentControl x:Name="ContentHost" Grid.Row="1" Margin="0,0,0,0">
                <TextBlock Text="{Binding Response.Explanation}" />
            </ContentControl>

        </Grid>
    </Border>
</Window>