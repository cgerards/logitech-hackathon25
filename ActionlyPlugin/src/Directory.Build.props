<Project>
    <PropertyGroup>
        <LangVersion>latest</LangVersion>
        <!-- Removed BaseIntermediateOutputPath and BaseOutputPath overrides to avoid collisions between project and solution obj/output directories which cause duplicate generated files -->
    </PropertyGroup>
</Project>
