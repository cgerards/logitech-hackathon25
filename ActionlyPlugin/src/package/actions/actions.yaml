# Example actions manifest for Actionly plugin
# List each action and reference the symbol by filename (without path). The runtime will look in package/actionsymbols/.

actions:
  - id: Actionly.Prompt
    class: Loupedeck.ActionlyPlugin.PromptCommand
    name: Prompt Button
    description: Opens the prompt dialog
    symbol: Loupedeck.ActionlyPlugin.PromptCommand.svg
    group: Commands
