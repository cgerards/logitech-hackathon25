<UserControl x:Class="Loupedeck.ActionlyPlugin.Views.DefaultView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Width="330" Height="120">

    <StackPanel Margin="2,2,2,2">
        <TextBlock Text="Welcome to the Agent!" Foreground="White" FontWeight="Bold" FontSize="14"/>
        <TextBlock Text="Please type in your program-specific request." Foreground="#FFD0D0D0" TextWrapping="Wrap" Margin="0,4,0,0"/>
        <!-- Text field with paperplane submit button -->
        <Grid Margin="0,2,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="36" />
            </Grid.ColumnDefinitions>

            <!-- Multiline textbox: 3 lines, accepts return, wraps text -->
            <TextBox x:Name="InnerInputTextBox" Grid.Column="0" MinWidth="180" MinLines="3" MaxLines="3" AcceptsReturn="True" TextWrapping="Wrap" Background="#FF1E1E1E" Foreground="White" BorderBrush="#FF3E3E42" Padding="6" VerticalContentAlignment="Top" />

            <Button x:Name="SendButton" Grid.Column="1" Width="32" Height="28" Margin="6,0,0,0" ToolTip="Send" Background="#FF2A8BF2" BorderBrush="#FF1F6FCF" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center" Click="SendButton_Click">
                <Path Data="M2,21 L23,12 L2,3 L8,12 L2,21 Z" Fill="White" Stretch="Uniform" Width="14" Height="14" RenderTransformOrigin="0.5,0.5">
                    <Path.RenderTransform>
                        <RotateTransform Angle="-35"/>
                    </Path.RenderTransform>
                </Path>
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}" CornerRadius="6">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Setter Property="Cursor" Value="Hand"/>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Opacity" Value="0.9"/>
                                <Setter Property="RenderTransform">
                                    <Setter.Value>
                                        <ScaleTransform ScaleX="1.03" ScaleY="1.03"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
        </Grid>
    </StackPanel>
</UserControl>