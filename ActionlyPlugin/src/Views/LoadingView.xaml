<UserControl x:Class="Loupedeck.ActionlyPlugin.Views.LoadingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Width="260" Height="110">

    <Border CornerRadius="6" Padding="8">
        <StackPanel>
            <TextBlock Text="AGENT STARTED" Foreground="White" FontWeight="Bold" FontSize="16" HorizontalAlignment="Center"/>
            <TextBlock Text="Please wait while the operation completes." Foreground="#FFD0D0D0" TextWrapping="Wrap" Margin="0,8,0,0"/>

            <!-- Spinner -->
            <Canvas Width="40" Height="40" HorizontalAlignment="Center" Margin="0,12,0,0">
                <Ellipse Width="36" Height="36" Stroke="#FF2A8BF2" StrokeThickness="4" StrokeDashArray="2 6" RenderTransformOrigin="0.5,0.5">
                    <Ellipse.RenderTransform>
                        <RotateTransform />
                    </Ellipse.RenderTransform>
                    <Ellipse.Triggers>
                        <EventTrigger RoutedEvent="FrameworkElement.Loaded">
                            <BeginStoryboard>
                                <Storyboard RepeatBehavior="Forever">
                                    <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(RotateTransform.Angle)" From="0" To="360" Duration="0:0:1" />
                                </Storyboard>
                            </BeginStoryboard>
                        </EventTrigger>
                    </Ellipse.Triggers>
                </Ellipse>
            </Canvas>

        </StackPanel>
    </Border>
</UserControl>